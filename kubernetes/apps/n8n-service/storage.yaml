---
# N8N PERSISTENT VOLUME CLAIM
# Shared by n8n (main) and n8n-worker deployments
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: n8n-pvc
  namespace: n8n-prod
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
